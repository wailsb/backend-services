// src/v1/routes/token.routes.ts
/*import { Router } from 'express';
import { refreshAccessToken, logout } from '../controllers/token.controller';

const router = Router();

router.post('/refresh-token', refreshAccessToken); // POST /api/v1/token/refresh-token
router.post('/logout', logout);                    // POST /api/v1/token/logout

export default router;
*/